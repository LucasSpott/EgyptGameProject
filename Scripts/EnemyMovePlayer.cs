using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyMovePlayer : MonoBehaviour
{
   /* [Header("Enemy Move Status")]
    [SerializeField] private Transform playerPosition;

    public float speed = 3.0f;

    void Start(){
        playerPosition = GameObject.FindGameObjectWithTag("Player").transform;
    }


    void Update(){
        SegPlayer();
    }

    private void SegPlayer(){
        if(playerPosition.GameObject != null){
        transform.position = Vector3.MoveTowards(transform.position, playerPosition.position, speed * Time.deltaTime);
        }
    }*/
}